import actionTypes from '../actions/actionTypes';

export default function rootReducer(state, action) {
    const {
        type,
        payload
    } = action;

    switch (type) {
        default:
            return state
    }
}